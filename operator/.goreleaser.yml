version: 2

project_name: furnace-operator

before:
  hooks:
    - go mod tidy

builds:
  - id: furnace-operator
    main: ./cmd/main.go
    binary: furnace-operator
    ldflags:
      - -s -w -X main.version={{ .Version }}
    goos:
      - linux
    goarch:
      - amd64
      - arm64

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "{{ .ProjectName }}_{{ .Version }}_SHA256SUMS"
  algorithm: sha256

release:
  github:
    owner: "{{ .Env.GITHUB_REPOSITORY_OWNER }}"
    name: furnace
  name_template: "operator/{{ .Tag }}"
  header: |
    ## Furnace Operator {{ .Tag }}

    Docker image: `ghcr.io/{{ .Env.GITHUB_REPOSITORY_OWNER }}/furnace-operator:{{ .Version }}`

changelog:
  disable: true
