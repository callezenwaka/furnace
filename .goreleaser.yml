version: 2

project_name: authpilot

before:
  hooks:
    - go mod tidy

builds:
  - id: authpilot
    main: ./server/cmd/authpilot
    binary: authpilot
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}

archives:
  - id: authpilot
    builds: [authpilot]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE*

checksum:
  name_template: "{{ .ProjectName }}_{{ .Version }}_checksums.txt"
  algorithm: sha256

release:
  github:
    owner: "{{ .Env.GITHUB_REPOSITORY_OWNER }}"
    name: authpilot
  draft: false
  prerelease: auto
  name_template: "{{ .Tag }}"
  header: |
    ## Authpilot {{ .Version }}

    Docker image: `ghcr.io/{{ .Env.GITHUB_REPOSITORY_OWNER }}/authpilot:{{ .Version }}`

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^chore:"
      - "^test:"
      - "Merge pull request"
      - "Merge branch"
