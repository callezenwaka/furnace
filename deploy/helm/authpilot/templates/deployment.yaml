apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "authpilot.fullname" . }}
  labels:
    {{- include "authpilot.labels" . | nindent 4 }}
  {{- with .Values.podAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "authpilot.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "authpilot.selectorLabels" . | nindent 8 }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: authpilot
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.image.repository }}:{{ include "authpilot.imageTag" . }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - --config=/etc/authpilot/config.yaml
          ports:
            - name: http
              containerPort: 8025
              protocol: TCP
            - name: protocol
              containerPort: 8026
              protocol: TCP
          env:
            {{- if or .Values.config.apiKey .Values.config.scimKey }}
            {{- if .Values.config.apiKey }}
            - name: AUTHPILOT_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "authpilot.fullname" . }}
                  key: AUTHPILOT_API_KEY
            {{- end }}
            {{- if .Values.config.scimKey }}
            - name: AUTHPILOT_SCIM_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "authpilot.fullname" . }}
                  key: AUTHPILOT_SCIM_KEY
            {{- end }}
            {{- end }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: config
              mountPath: /etc/authpilot
              readOnly: true
            {{- if .Values.persistence.enabled }}
            - name: data
              mountPath: {{ .Values.persistence.mountPath }}
            {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "authpilot.fullname" . }}
        {{- if .Values.persistence.enabled }}
        - name: data
          persistentVolumeClaim:
            claimName: {{ include "authpilot.fullname" . }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
